	<%= render 'layouts/header' %>
<div id="trading_floor">
		<br>
		<br>		
		<p class="notice"><%= notice %></p> 
		<p class="alert"><%= alert %></p>
			<%= yield %>
		</div>
	<%= render 'layouts/footer' %>
</div>